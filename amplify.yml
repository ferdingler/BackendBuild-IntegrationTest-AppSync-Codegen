version: 1
env:
  _LIVE_UPDATES: [{"pkg":"@aws-amplify/cli","type":"npm","version":"5.1.2"}]
  USER_BRANCH: dev
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands:
        - echo "true"
  artifacts:
    baseDirectory: src